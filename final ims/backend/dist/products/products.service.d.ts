import { JsonDbService } from '../common/json-db.service';
import { ProductFeedback, ProductRecord, RatingBreakdown, StoreInventoryEntry, TransactionRecord } from '../common/database.types';
import { CreateProductDto } from './dto/create-product.dto';
import { CreateProductFeedbackDto } from './dto/create-product-feedback.dto';
import { UpdateProductDto } from './dto/update-product.dto';
export declare class ProductsService {
    private readonly db;
    constructor(db: JsonDbService);
    findAll(retailerId?: string, storeId?: string): ProductRecord[];
    findOne(id: string, retailerId?: string, storeId?: string): ProductRecord;
    findBySku(sku: string, retailerId?: string, storeId?: string): ProductRecord;
    create(createProductDto: CreateProductDto): ProductRecord;
    update(id: string, updateProductDto: UpdateProductDto): ProductRecord;
    remove(id: string): {
        message: string;
        item: ProductRecord;
    };
    getFeedback(sku: string, retailerId?: string): ProductFeedback[];
    getRatingSummary(sku: string, retailerId?: string): {
        avg: number;
        total: number;
        breakdown: RatingBreakdown;
    };
    addFeedback(sku: string, createProductFeedbackDto: CreateProductFeedbackDto, retailerId?: string): ProductFeedback;
    rebuildInventoryFromTransactions(transactions: TransactionRecord[]): {
        qty: number;
        price: number;
        revenue: number;
        storeInventory: {
            storeId: string;
            qty: number;
        }[];
        id: string;
        retailerId?: string;
        sku: string;
        name: string;
        category: string;
        subcategory: string;
        subCategory?: string;
        brand: string;
        priceUSD: number;
        cost?: number;
        discountType?: string;
        discountValue?: number;
        finalPrice?: number;
        taxRate?: number;
        taxType?: string;
        unit: string;
        initialQty: number;
        min: number;
        max: number;
        creator: string;
        creatorImg: string;
        productImg: string;
        galleryImages: string[];
        images: string[];
        emoji: string;
        soldThisMonth: number;
        trend: import("../common/database.types").ProductTrend | string;
        description: string;
        descriptionHtml?: string;
        supplier: string;
        supplierSku?: string;
        barcode?: string;
        warehouse?: string;
        bin?: string;
        reorderPoint?: number;
        reorderQty?: number;
        hasVariants?: boolean;
        variants?: Array<Record<string, unknown>>;
        hasExpiry?: boolean;
        expiryDate?: string;
        batchNumber?: string;
        weightKg?: number;
        lengthCm?: number;
        widthCm?: number;
        heightCm?: number;
        shippingClass?: string;
        customFields?: Array<Record<string, unknown>>;
        tags?: string[];
        highlights: string[];
        specs: Record<string, string>;
        palette: string[];
        initialStoreInventory: StoreInventoryEntry[];
        feedback: ProductFeedback[];
        ratingBreakdown: RatingBreakdown;
        ratingCount: number;
        ratingAvg: number;
        status: import("../common/database.types").ProductStatus | string;
        visibility: import("../common/database.types").ProductVisibility | string;
        createdAt: string;
        updatedAt: string;
        restockedAt?: string;
        lastStockChangeAt?: string;
    }[];
    applyInventoryAdjustment(sku: string, qtyDelta: number, preferredStoreId?: string, retailerId?: string): ProductRecord;
    private getProducts;
    private saveProducts;
    private filterProducts;
    private normalizeStoredProduct;
    private buildProductRecord;
    private resolveStoreInventory;
    private resolveInitialStoreInventory;
    private normalizeStoreInventory;
    private adjustStoreInventory;
    private buildImageSet;
    private normalizeImageList;
    private buildProductPlaceholder;
    private buildAvatarSvg;
    private escapeXml;
    private normalizeRatingBreakdown;
    private applyRatings;
    private normalizeSpecs;
    private resolveFinalPrice;
    private normalizeSku;
    private ensureUniqueSku;
    private generateNextSku;
    private projectProductForStore;
    private matchesRetailerScope;
    private isSameRetailerScope;
    private resolveStoreId;
    private normalizeText;
    private pickNumber;
    private toSafeInteger;
    private toMoney;
    private toOptionalMoney;
}
