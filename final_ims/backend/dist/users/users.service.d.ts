import { User } from './user.entity';
import { CreateUserDto } from './dto/create-user.dto';
import { UpdateUserDto } from './dto/update-user.dto';
type PublicUser = Omit<User, 'password'>;
export declare class UsersService {
    private readonly users;
    private readonly dataDirectory;
    private readonly dataFile;
    private readonly retailersFile;
    private readonly suppliersFile;
    private readonly billersFile;
    private readonly dbFile;
    constructor();
    findAll(role?: string, email?: string): PublicUser[];
    findOne(id: string): PublicUser;
    create(createUserDto: CreateUserDto): PublicUser;
    update(id: string, updateUserDto: UpdateUserDto): PublicUser;
    updateProfile(id: string, profile: Record<string, unknown>): PublicUser;
    remove(id: string): boolean;
    login(email: string, password: string): PublicUser;
    private syncLinkedProfileForUserId;
    private hydrateLinkedProfile;
    private hydrateRetailerProfile;
    private hydrateSupplierProfile;
    private hydrateBillerProfile;
    private findRetailerProfile;
    private findSupplierProfile;
    private findBillerProfile;
    private normalizeRetailerStores;
    private buildRetailerProfileSummary;
    private buildSupplierProfileSummary;
    private readRecordsFromFile;
    private readCollectionFromDbFile;
    private hasLinkedProfileChanges;
    private findStoredUser;
    private readAll;
    private writeAll;
    private loadFromDisk;
    private persistToDisk;
    private normalizeStoredUser;
    private toPublicUser;
    private normalizeRole;
    private normalizeEmail;
    private normalizeStringList;
    private normalizeProfile;
    private normalizeText;
}
export {};
